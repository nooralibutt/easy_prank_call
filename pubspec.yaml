name: easy_prank_call
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: '>=2.19.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.3
  audioplayers: ^0.20.1
  flutter_spinkit: ^5.1.0
  flutter_svg: ^2.0.0+1
  video_player: ^2.5.1
  vibration: ^1.7.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

# The following section is specific to Flutter packages.
flutter:

  assets:
    - assets/
    - assets/audio/
    - assets/images/
    - assets/videos/
